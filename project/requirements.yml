---
collections:
  # required for firewalld, selinux, etc.
  # https://galaxy.ansible.com/ansible/posix
  - name: ansible.posix
    version: 1.4.0
  # https://galaxy.ansible.com/community/docker
  - name: community.docker
    version: 2.6.0
  # https://galaxy.ansible.com/community/mysql
  - name: community.mysql
    version: 3.3.0
